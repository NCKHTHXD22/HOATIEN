# ============================================================
# BACKEND ENV - Xã Hoa Tiến Hybrid DB
# Sao chép file này thành .env và điền thông tin thực
# ============================================================

# --- SERVER ---
PORT=3000
NODE_ENV=development

# --- PostgreSQL (Railway hoặc local) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/hoa_tien

# --- MongoDB (Atlas hoặc local) ---
# Format: mongodb+srv://USER:PASSWORD@cluster.mongodb.net/
# Hoặc local: mongodb://localhost:27017
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/
MONGODB_DB_NAME=hoa_tien

# --- JWT ---
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# --- Zalo OA ---
ZALO_APP_ID=your_zalo_app_id
ZALO_APP_SECRET=your_zalo_app_secret
ZALO_OA_ACCESS_TOKEN=your_zalo_oa_access_token
ZALO_WEBHOOK_SECRET=your_zalo_webhook_secret

# --- CORS (danh sách domain frontend được phép) ---
CORS_ORIGINS=http://localhost:5173,http://localhost:3001
